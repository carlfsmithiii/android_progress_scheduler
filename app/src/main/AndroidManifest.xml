<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.wgu.csmi508.progressscheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="edu.wgu.csmi508.progressscheduler.HomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CourseListActivity" />
        <activity android:name=".CoursePagerActivity" />
        <activity android:name=".CourseEditActivity" />
        <activity android:name=".MentorPagerActivity" />
        <activity android:name=".MentorListActivity" />
        <activity android:name=".TermListActivity" />
        <activity android:name=".TermEditActivity" />
        <activity android:name=".TermPagerActivity" />
        <activity android:name=".MentorSelectorActivity" />
        <activity android:name=".CourseAssessmentActivity" />
        <activity android:name=".CourseAssessmentListActivity"/>
        <activity android:name=".NoteListActivity" />
        <activity android:name=".NoteEditActivity" />
        <activity android:name=".AssessmentListActivity" />
        <activity android:name=".PictureActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="edu.wgu.csmi508.progressscheduler.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files" />
        </provider>
    </application>

</manifest>